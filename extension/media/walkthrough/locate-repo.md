## Locate the Plugin Repo

The extension needs to know where your **CursorPlugins** repository lives on disk. It will look for a folder named `CursorPlugins` next to your workspace automatically.

If auto-detection doesn't find it, click the button below to browse for it manually. The path is saved in your global settings so you only need to do this once.
